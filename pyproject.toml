[project]
name = "lifeops"
version = "0.1.0"
description = "LifeOps backend foundation"
requires-python = ">=3.13,<3.14"
dependencies = [
    "psycopg[binary]>=3.3.3,<4",
]

[build-system]
requires = ["hatchling>=1.27.0"]
build-backend = "hatchling.build"

[tool.uv]
package = true
